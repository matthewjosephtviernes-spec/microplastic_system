# requirements.txt  (Python >=3.9)

# Core
numpy>=1.23
pandas>=1.5
scikit-learn>=1.3
matplotlib>=3.6
joblib>=1.2

# Parquet (optional; remove if only CSV)
pyarrow>=12

# Imbalance handling (SMOTE). Remove if not needed.
imbalanced-learn>=0.10

# Optional extra model
xgboost>=1.7
