# requirements.txt  — Python 3.9–3.13

# UI
streamlit>=1.29

# Core data stack
numpy>=1.23
pandas>=1.5
scikit-learn>=1.3
matplotlib>=3.6
joblib>=1.2

# File formats
pyarrow>=12          # Parquet support
openpyxl>=3.1        # Excel .xlsx support

# Class imbalance (SMOTE)
imbalanced-learn>=0.10

# Robust CSV encoding detection (fixes UnicodeDecodeError)
charset-normalizer>=3.3
