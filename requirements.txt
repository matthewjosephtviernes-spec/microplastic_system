# requirements.txt  (Python >=3.9)

# Core
numpy>=1.23
pandas>=1.5
scikit-learn>=1.3
matplotlib>=3.6
joblib>=1.2
pyarrow>=12        # only if you read Parquet

# Imbalance (SMOTE) — optional; remove if you don't need it
imbalanced-learn>=0.10
